---
/**
 * Gabarit commun. Voir SPEC.md, section 9.5 : le sujet, ce sont les photos
 * et le texte, pas l'interface. Le style complet arrive au commit 5 ; ici,
 * seulement de quoi lire confortablement.
 */
interface Props {
  titre: string;
  description?: string;
}
const { titre, description } = Astro.props;
---

<!doctype html>
<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex, nofollow" />
    <title>{titre}</title>
    {description && <meta name="description" content={description} />}
    <style>
      :root {
        color-scheme: light dark;
        --fond: #fbf9f6;
        --encre: #2b2723;
        --discret: #6f665e;
        --accent: #c0562a;
        --trait: #e3ddd5;
      }
      @media (prefers-color-scheme: dark) {
        :root {
          --fond: #16130f;
          --encre: #eae4dc;
          --discret: #a79c91;
          --trait: #322c26;
        }
      }
      * {
        box-sizing: border-box;
      }
      body {
        margin: 0;
        background: var(--fond);
        color: var(--encre);
        font: 1.05rem/1.65 ui-serif, Georgia, "Times New Roman", serif;
      }
      main {
        max-width: 46rem;
        margin: 0 auto;
        padding: 2rem 1.25rem 6rem;
      }
      a {
        color: var(--accent);
      }
      h1,
      h2,
      h3 {
        font-family: ui-sans-serif, system-ui, sans-serif;
        line-height: 1.2;
        letter-spacing: -0.01em;
      }
      h1 {
        font-size: clamp(1.7rem, 5vw, 2.4rem);
        margin: 0 0 0.3rem;
      }
      .discret {
        color: var(--discret);
        font-family: ui-sans-serif, system-ui, sans-serif;
        font-size: 0.9rem;
      }
      nav.fil {
        border-bottom: 1px solid var(--trait);
        padding: 0.9rem 1.25rem;
        font-family: ui-sans-serif, system-ui, sans-serif;
        font-size: 0.9rem;
      }
      nav.fil a {
        text-decoration: none;
      }

      /* Section 9.3 : une colonne en dessous de 1024 pixels, et la lecture
         reste le sujet. La mise en page à deux colonnes arrive avec la carte,
         au lot 5 : une colonne vide en attendant donnerait une page cassée. */
      @media (min-width: 48rem) {
        main {
          padding: 3rem 2rem 8rem;
        }
      }

      p {
        text-wrap: pretty;
      }
      h1,
      h2,
      h3 {
        text-wrap: balance;
      }
      img {
        max-width: 100%;
      }

      footer {
        border-top: 1px solid var(--trait);
        margin-top: 4rem;
        padding: 1.5rem 1.25rem 3rem;
        text-align: center;
        color: var(--discret);
        font-family: ui-sans-serif, system-ui, sans-serif;
        font-size: 0.82rem;
      }

      /* Le lien est visible au clavier, y compris sur les vignettes. */
      a:focus-visible,
      button:focus-visible,
      [data-grand]:focus-visible {
        outline: 2px solid var(--accent);
        outline-offset: 3px;
      }
    </style>
  </head>
  <body>
    <nav class="fil"><a href="/">Voyages</a></nav>
    <main>
      <slot />
    </main>
    <footer>Journal de voyage de la famille. Lien privé, page non indexée.</footer>
  </body>
</html>
